import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	init: {
		display: "flex",
		flexDirection: "column",
		backgroundColor: "#fff",
		alignItems: "center",
	},
	initTitle: {
		fontSize: 24,
		fontWeight: "bold",
		color: "orange",
	},
});
